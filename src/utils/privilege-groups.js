export const privilegeGroups=['CRM','Events','Documents','Finance','Communications','Website','Operations','Reporting','Administration'];
export function privilegeGroup(key){if(/sales|leads|clients/.test(key))return 'CRM';if(/proposal|invoice|document/.test(key))return 'Documents';if(/finance|payment|refund/.test(key))return 'Finance';if(/communications|templates|automations|email|sms/.test(key))return 'Communications';if(/website|content|media/.test(key))return 'Website';if(/operations|staff|equipment|tasks|attendant|gallery/.test(key))return 'Operations';if(/event|calendar/.test(key))return 'Events';if(/analytics|dashboard|report/.test(key))return 'Reporting';return 'Administration';}
export function grantablePrivileges(permissions,actor){return permissions.filter(p=>p.key!=='*'&&!/root/i.test(p.key)&&(actor.permissions?.includes('*')||actor.permissions?.includes(p.key)));}
