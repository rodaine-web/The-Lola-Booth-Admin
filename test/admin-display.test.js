import test from 'node:test';import assert from 'node:assert/strict';
import {execFileSync} from 'node:child_process';
import {formatDateOnly,formatTimestamp,formatMoney,formatDisplay,statusTone,businessToday} from '../src/utils/display.js';
test('date-only December 16 remains December 16 in negative and positive offsets',()=>{for(const TZ of ['America/Chicago','America/Los_Angeles','UTC','Pacific/Kiritimati']){const output=execFileSync(process.execPath,['--input-type=module','-e',"import {formatDateOnly} from './src/utils/display.js'; console.log(formatDateOnly('2027-12-16'));"],{env:{...process.env,TZ},encoding:'utf8'}).trim();assert.equal(output,'December 16, 2027',TZ);}});
test('date-only input validates calendar and handles date API ISO without timezone drift',()=>{assert.equal(formatDateOnly('2027-12-16T00:00:00.000Z'),'December 16, 2027');assert.equal(formatDateOnly('2027-02-30'),'—');assert.equal(formatDateOnly(null),'—');});
test('timestamps retain instant semantics and business timezone',()=>{assert.match(formatTimestamp('2027-12-16T00:00:00Z'),/Dec 15, 2027.*6:00 PM CST/);assert.equal(businessToday(new Date('2027-12-16T00:00:00Z')),'2027-12-15');});
test('money has consistent cents including zero and excessive precision',()=>{assert.equal(formatMoney(2877.6),'$2,877.60');assert.equal(formatMoney('999.2000000000000000'),'$999.20');assert.equal(formatMoney(0),'$0.00');assert.equal(formatMoney(null),'—');assert.equal(formatDisplay(false,'is_most_popular'),'No');assert.equal(formatDisplay(null,'proposal_number'),'Legacy record · number missing');});
test('status colors reflect operational meaning',()=>{assert.equal(statusTone('READY'),'success');assert.equal(statusTone('BLOCKED'),'danger');assert.equal(statusTone('PENDING'),'warning');assert.equal(statusTone('DRAFT'),'neutral');});
import {invoiceBalance,normalizeInvoice} from '../shared/invoice-balance.js';
test('invoice balance preserves zero, legacy fallback, void and absent item history',()=>{assert.equal(invoiceBalance({amount_outstanding:0,balance_due:499}),0);assert.equal(invoiceBalance({amount_outstanding:null,balance_due:'499.00'}),499);assert.equal(invoiceBalance({total:100,amount_paid:30}),70);assert.equal(invoiceBalance({status:'VOID',amount_outstanding:90}),0);assert.equal(normalizeInvoice({items:[],total:499,balance_due:499}).data_quality,'INCOMPLETE_HISTORICAL');});
import {grantablePrivileges,privilegeGroup} from '../src/utils/privilege-groups.js';
test('privilege groups exclude ungrantable access even for owner',()=>{const all=[{key:'*'},{key:'root.manage'},{key:'read:sales'},{key:'write:finance'}];assert.deepEqual(grantablePrivileges(all,{permissions:['read:sales']}).map(p=>p.key),['read:sales']);assert.equal(grantablePrivileges(all,{permissions:['*']}).length,2);assert.equal(privilegeGroup('communications.send'),'Communications');});
